# Terminus API Configuration
TERMINUS_API_URL=http://localhost:8080
# Global device ID (optional - overridden by kid-specific device IDs)
TERMINUS_DEVICE_ID=

# Development Configuration
NODE_ENV=development

# Authentication
# Default admin password (change in production)
ADMIN_PASSWORD=admin123